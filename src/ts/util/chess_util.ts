// Returns lists of the white and black pieces  
export function getPieces(board, includeWhite, includeBlack) {
    let pieces = [];
    for (let i = 0; i < 8; i++) {
        // TODO pick up work here. should make a piece class constructor to fit
        // for (let j = 0; j < 8; j++) {
        //     if ()
        // }
    }
    // console.log(chess.board);
}